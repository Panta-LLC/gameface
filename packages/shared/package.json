{
  "name": "@gameface/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "vitest --watch",
    "test": "vitest run",
    "lint": "eslint src --ext .ts,.tsx",
    "format": "prettier -w .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}